========
Comments
========

// hello world

test {
  // hello world
  italic: true;
}

// hello world

---

(stylesheet
  (comment)
  (rule
    (selectors 
      (selector (node (kind (name)))))
    (styles 
      (comment) 
      (style (name) (value (boolean)))))
  (comment))
