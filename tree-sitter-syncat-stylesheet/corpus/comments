========
Comments
========

// hello world

test {
  // hello world
  italic: true;
}

// hello world

---

(stylesheet
  (comment)
  (rule
    (selectors 
      (selector (scope (node (kind (name))))))
    (styles 
      (comment) 
      (style (name) (value (boolean)))))
  (comment))
