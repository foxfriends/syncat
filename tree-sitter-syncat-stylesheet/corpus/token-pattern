==============
Token patterns
==============

block /hello_.*/i {
  color: white;
}

---

(stylesheet
  (item
    (rule 
      (selectors
        (selector (scope (node (kind (name))) (node (token (regex))))))
      (styles
        (style (name) (value (color (named_color))))))))

