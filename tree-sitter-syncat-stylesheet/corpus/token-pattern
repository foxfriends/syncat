==============
Token patterns
==============

block /hello_.*/i {
  color: white;
}

---

(stylesheet
  (rule 
    (selectors
      (selector (node (kind (name))) (node (token (regex)))))
    (styles
      (style (name) (value (color (named_color)))))))
