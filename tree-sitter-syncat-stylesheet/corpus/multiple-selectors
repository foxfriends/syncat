==================
Multiple selectors
==================

hello world, second selector {}

---

(stylesheet
  (rule 
    (selectors
      (selector (scope (node (kind (name))) (node (kind (name)))))
      (selector (scope (node (kind (name))) (node (kind (name))))))
    (styles)))
