============
Named groups
============

root (<cap> group "code") + hello {}

---

(stylesheet
  (rule
    (selectors
      (selector
        (node (kind (name)))
        (node (group (group_name (name)) (selector (node (kind (name))) (node (token (string))))))
        (node_modifier (direct_sibling))
        (node (kind (name)))))
    (styles)))
