==================
Any child selector
==================

root * hello { }

---

(stylesheet
  (item
    (rule
      (selectors (selector (scope (node (kind (name))) (node (any)) (node (kind (name))))))
      (styles))))
