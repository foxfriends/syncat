======
Groups
======

root (group "code") + hello {}

---

(stylesheet
  (rule
    (selectors
      (selector
        (node (kind (name)))
        (node (group (selector (node (kind (name))) (node (token (string))))))
        (node_modifier (direct_sibling))
        (node (kind (name)))))
     (styles)))
